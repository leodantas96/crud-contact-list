
<!-- include header -->
<%- include('include/_header') %>
<!-- /include header -->

  <!-- Main Site -->
  <main id="site-main">
    <div class="container">
        <div class="box-nav d-flex justify-between">
           <div class="filter">
               <a href="/"><i class="fas fa-angle-double-left"></i> Todos os contactos</a>
           </div>
        </div>
        <div class="form-title text-center">
            <h2 class="text-dark">Editar contacto</h2>
            <span class="text-light">Edite os campos abaixo que deseja alterar</span>
        </div>

        <!-- add user form -->
         <!-- form handling -->
 <form method="POST" id="update_user">
    <div class="new_user">
        <div class="form-group">
            <label for="name" class="text-light">Nome</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="name" value="<%= user.name %>">
        </div>
        <div class="form-group">
            <label for="Email" class="text-light">Email</label>
            <input type="text" name="email" value="<%= user.email%>">
        </div>
        <div class="form-group">
            <label for="phone" class="text-light">Contacto</label>
            <input type="text" name="phone" value="<%= user.phone%>">
        </div>
        <div class="form-group">
            <label for="country" class="text-light">País</label>
            <input type="text" name="country" value="<%= user.country%>">
        </div>

        <div class="form-group">
            <button type="submit" class="btn text-dark update">Guardar</button>
        </div>

    </div>
</form>
       
    </div>
</main>
<!-- /Main Site -->

<!-- include footer -->
<%- include('include/_footer') %>
<!-- /include footer -->

